<div class="container">
         <table>
        <thead>
          <tr>
              <th>Item Id</th>
              <th>Item Name</th>
              <th>Item Price</th>
          </tr>
        </thead>

        <tbody>
            {{#each products}}
          <tr>
            <td>{{this.id}}</td>
            <td>{{this.title}}</td>
            <td>${{this.price}}</td>
          </tr>
          {{/each}}
        </tbody>
      </table>
</div>